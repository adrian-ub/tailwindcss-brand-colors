import { tailwindcssBrandColors } from './tailwindcss-brand-colors';

describe('tailwindcssBrandColors', () => {
  it('should work', () => {
    expect(tailwindcssBrandColors()).toEqual('tailwindcss-brand-colors');
  });
});
